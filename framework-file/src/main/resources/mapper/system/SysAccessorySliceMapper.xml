<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.system.mapper.SysAccessorySliceMapper">
    <select id="selectByMainId" parameterType="java.lang.String"
            resultType="cn.silver.framework.system.domain.SysAccessorySlice">
        SELECT *
        FROM sys_accessory_slice
        WHERE accessory = #{mainId}
    </select>
    <delete id="deleteByMainId" parameterType="java.lang.String">
        DELETE
        FROM sys_accessory_slice
        WHERE accessory = #{mainId}
    </delete>
</mapper>
