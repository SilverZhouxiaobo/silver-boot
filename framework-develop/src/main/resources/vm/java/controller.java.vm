package ${packageName}.controller;

#if($table.crud)
    #set($Controller="DataController")
#elseif($table.data || $table.sub)
    #set($Controller="DataController")
#elseif($table.tree)
    #set($Controller="TreeController")
#end

import io.swagger.annotations.Api;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;
    $Controller;
import ${packageName}.domain.${ClassName};
import ${packageName}.service.I${ClassName}Service;

/**
 * ${functionName}Controller
 *
 * @author ${author}
 * @date ${datetime}
 */

@RestController
@Api(tags = {"${functionName}"})
@RequestMapping("/${moduleName}/${businessName}")
public class ${ClassName}Controller extends $Controller<I${ClassName}Service,${ClassName}> {

public ${ClassName}Controller(){
        this.authorize="${permissionPrefix}";
        this.title="${functionName}";
        }
        }
