<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.workflow.mapper.FlowCategoryMapper">
    <resultMap id="BaseResultMap" type="cn.silver.framework.workflow.domain.FlowCategory">
        <id column="id" jdbcType="VARCHAR" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="code" jdbcType="VARCHAR" property="code"/>
        <result column="order_no" jdbcType="INTEGER" property="orderNo"/>
        <result column="remark" jdbcType="VARCHAR" property="remark"/>
        <result column="create_by" jdbcType="VARCHAR" property="createBy"/>
        <result column="create_name" jdbcType="VARCHAR" property="createName"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_by" jdbcType="VARCHAR" property="updateBy"/>
        <result column="update_name" jdbcType="VARCHAR" property="updateName"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
        <result column="deleted" jdbcType="VARCHAR" property="deleted"/>
    </resultMap>

    <!-- 如果有逻辑删除字段过滤，请写到这里 -->
    <sql id="filterRef">
        <!-- 这里必须加上全包名，否则当filterRef被其他Mapper.xml包含引用的时候，就会调用Mapper.xml中的该SQL片段 -->
        <include refid="cn.silver.framework.workflow.mapper.FlowCategoryMapper.inputFilterRef"/>
    </sql>

    <!-- 这里仅包含调用接口输入的主表过滤条件 -->
    <sql id="inputFilterRef">
        <if test="flowCategoryFilter != null">
            <if test="flowCategoryFilter.name != null and flowCategoryFilter.name != ''">
                AND flow_category.name = #{flowCategoryFilter.name}
            </if>
            <if test="flowCategoryFilter.code != null and flowCategoryFilter.code != ''">
                AND flow_category.code = #{flowCategoryFilter.code}
            </if>
        </if>
    </sql>

    <select id="getFlowCategoryList" resultMap="BaseResultMap"
            parameterType="cn.silver.framework.workflow.domain.FlowCategory">
        SELECT * FROM flow_category
        <where>
            <include refid="filterRef"/>
        </where>
        <if test="orderBy != null and orderBy != ''">
            ORDER BY ${orderBy}
        </if>
    </select>
</mapper>
