<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.config.mapper.SysDictTypeMapper">

    <resultMap type="cn.silver.framework.config.domain.SysDictType" id="SysDictTypeResult">
        <id property="id" column="id"/>
        <result property="dictName" column="dict_name"/>
        <result property="dictType" column="dict_type"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createName" column="create_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateName" column="update_name"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectDictTypeVo">
        select id,
               dict_name,
               dict_type,
               status,
               create_by,
               create_name,
               create_time,
               update_by,
               update_name,
               update_time,
               remark
        from sys_dict_type
    </sql>

    <select id="selectDictTypeList" parameterType="cn.silver.framework.config.domain.SysDictType"
            resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        <where>
            <if test="dictName != null and dictName != ''">
                AND dict_name like concat(concat('%', #{dictName}), '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="dictType != null and dictType != ''">
                AND dict_type like concat(concat('%', #{dictType}), '%')
            </if>
            <if test="beginTime != null and beginTime != ''"><!-- 开始时间检索 -->
                and to_date(to_char(create_time,'yyyy-mm-dd'),'yyyy-mm-dd') &gt;= to_date(#{beginTime},'yyyy-mm-dd')
            </if>
            <if test="endTime != null and endTime != ''"><!-- 结束时间检索 -->
                and to_date(to_char(create_time,'yyyy-mm-dd'),'yyyy-mm-dd') &lt;= to_date(#{endTime},'yyyy-mm-dd')
            </if>
        </where>
    </select>

    <select id="selectDictTypeAll" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
    </select>

    <select id="selectDictTypeById" parameterType="java.lang.String" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        where id = #{id}
    </select>

    <select id="selectDictTypeByType" parameterType="java.lang.String" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        where dict_type = #{dictType}
    </select>

    <select id="checkDictTypeUnique" parameterType="java.lang.String" resultMap="SysDictTypeResult">
        <include refid="selectDictTypeVo"/>
        where dict_type = #{dictType}
    </select>

    <delete id="deleteDictTypeById" parameterType="java.lang.String">
        delete
        from sys_dict_type
        where id = #{id}
    </delete>

    <delete id="deleteDictTypeByIds" parameterType="java.lang.String">
        delete from sys_dict_type where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="updateDictType" parameterType="cn.silver.framework.config.domain.SysDictType">
        update sys_dict_type
        <set>
            <if test="dictName != null and dictName != ''">dict_name = #{dictName},</if>
            <if test="dictType != null and dictType != ''">dict_type = #{dictType},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateName != null and updateName != ''">update_name = #{updateName},</if>
            update_time = sysdate
        </set>
        where id = #{id}
    </update>

    <insert id="insertDictType" parameterType="cn.silver.framework.config.domain.SysDictType">
        insert into sys_dict_type(
        id,
        <if test="dictName != null and dictName != ''">dict_name,</if>
        <if test="dictType != null and dictType != ''">dict_type,</if>
        <if test="status != null">status,</if>
        <if test="remark != null and remark != ''">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        <if test="createName != null and createName != ''">create_name,</if>
        <if test="updateBy != null and updateBy != ''">update_by,</if>
        <if test="updateName != null and updateName != ''">update_name,</if>
        create_time,update_time
        )values(
        #{id},
        <if test="dictName != null and dictName != ''">#{dictName},</if>
        <if test="dictType != null and dictType != ''">#{dictType},</if>
        <if test="status != null">#{status},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        <if test="createName != null and createName != ''">#{createName},</if>
        <if test="updateBy != null and updateBy != ''">#{updateBy},</if>
        <if test="updateName != null and updateName != ''">#{updateName},</if>
        sysdate,sysdate
        )
    </insert>

</mapper> 