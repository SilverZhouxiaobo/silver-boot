<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.system.mapper.SysUserPostMapper">

    <resultMap type="cn.silver.framework.system.domain.SysUserPost" id="SysUserPostResult">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="postId" column="post_id"/>
    </resultMap>

    <delete id="deleteUserPostByUserId" parameterType="java.lang.String">
        delete
        from sys_user_post
        where user_id = #{userId}
    </delete>

    <select id="countUserPostById" resultType="java.lang.Integer">
        select count(1)
        from sys_user_post
        where post_id = #{postId}
    </select>

    <delete id="deleteUserPost" parameterType="java.lang.String">
        delete from sys_user_post where user_id in
        <foreach collection="array" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <insert id="batchUserPost" useGeneratedKeys="false">
        insert all
        <foreach item="item" index="index" collection="list">
            into sys_user_post(id,user_id, post_id) values
            (#{item.id},#{item.userId},#{item.postId})
        </foreach>
        select 1 from dual
    </insert>

</mapper> 