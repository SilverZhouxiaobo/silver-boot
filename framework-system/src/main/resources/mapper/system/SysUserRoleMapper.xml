<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.system.mapper.SysUserRoleMapper">

    <resultMap type="cn.silver.framework.system.domain.SysUserRole" id="SysUserRoleResult">
        <result property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="roleId" column="role_id"/>
    </resultMap>

    <select id="selectUserRoleByUserId" parameterType="java.lang.String" resultMap="SysUserRoleResult">
        select id, user_id, role_id
        from sys_user_role
        where user_id = #{userId}
    </select>

    <delete id="deleteUserRoleByUserId" parameterType="java.lang.String">
        delete
        from sys_user_role
        where user_id = #{userId}
    </delete>

    <select id="countUserRoleByRoleId" resultType="java.lang.Integer">
        select count(1)
        from sys_user_role
        where role_id = #{roleId}
    </select>

    <delete id="deleteUserRole" parameterType="java.lang.String">
        delete from sys_user_role where user_id in
        <foreach collection="array" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>

    <insert id="batchUserRole" parameterType="cn.silver.framework.system.domain.SysUserRole">
        insert all
        <foreach item="item" index="index" collection="list">
            into sys_user_role(id,user_id, role_id) values
            (#{item.id},#{item.userId},#{item.roleId})
        </foreach>
        select 1 from dual
    </insert>

    <delete id="deleteUserRoleInfo" parameterType="cn.silver.framework.system.domain.SysUserRole">
        delete
        from sys_user_role
        where user_id = #{userId}
          and role_id = #{roleId}
    </delete>

    <delete id="deleteUserRoleInfos">
        delete from sys_user_role where role_id=#{roleId} and user_id in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
    </delete>
</mapper> 