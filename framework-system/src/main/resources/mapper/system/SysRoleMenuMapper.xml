<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.system.mapper.SysRoleMenuMapper">

    <resultMap type="cn.silver.framework.system.domain.SysRoleMenu" id="SysRoleMenuResult">
        <result property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="menuId" column="menu_id"/>
    </resultMap>

    <select id="checkMenuExistRole" resultType="java.lang.Integer">
        select count(1)
        from sys_role_menu
        where menu_id = #{menuId}
    </select>

    <delete id="deleteRoleMenuByRoleId" parameterType="java.lang.String">
        delete
        from sys_role_menu
        where role_id = #{roleId}
    </delete>

    <delete id="deleteRoleMenu" parameterType="java.lang.String">
        delete from sys_role_menu where role_id in
        <foreach collection="col" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </delete>

    <insert id="batchRoleMenu">
        insert all
        <foreach item="item" index="index" collection="list">
            into sys_role_menu
            (id,role_id, menu_id) values
            (#{item.id, jdbcType=VARCHAR},#{item.roleId, jdbcType=VARCHAR},#{item.menuId, jdbcType=VARCHAR})
        </foreach>
        select 1 from dual
    </insert>

</mapper> 