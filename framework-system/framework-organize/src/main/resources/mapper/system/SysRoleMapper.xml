<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.system.mapper.SysRoleMapper">

    <resultMap type="cn.silver.framework.system.domain.SysRole" id="SysRoleResult">
        <id property="id" column="id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="menuCheckStrictly" column="menu_check_strictly"/>
        <result property="deptCheckStrictly" column="dept_check_strictly"/>
        <result property="status" column="status"/>
        <result property="createBy" column="create_by"/>
        <result property="createName" column="create_name"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateName" column="update_name"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectRoleVo">
        select distinct r.id,
                        r.role_name,
                        r.role_key,
                        r.role_sort,
                        r.data_scope,
                        r.menu_check_strictly,
                        r.dept_check_strictly,
                        r.status,
                        r.create_time,
                        r.remark
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.id
                 left join sys_user u on u.id = ur.user_id
                 left join sys_dept d on u.dept_id = d.id
    </sql>

    <select id="selectRoleList" parameterType="cn.silver.framework.system.domain.SysRole" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        <where>
            <if test="id != null and id != 0">
                AND r.id = #{id}
            </if>
            <if test="roleName != null and roleName != ''">
                AND r.role_name like concat(concat('%', #{roleName}), '%')
            </if>
            <if test="status != null and status != ''">
                AND r.status = #{status}
            </if>
            <if test="roleKey != null and roleKey != ''">
                AND r.role_key like concat(concat('%', #{roleKey}), '%')
            </if>
            <if test="beginTime != null and beginTime != ''"><!-- 开始时间检索 -->
                and to_date(to_char(r.create_time,'yyyy-mm-dd'),'yyyy-mm-dd') &gt;= to_date(#{beginTime},'yyyy-mm-dd')
            </if>
            <if test="endTime != null and endTime != ''"><!-- 结束时间检索 -->
                and to_date(to_char(r.create_time,'yyyy-mm-dd'),'yyyy-mm-dd') &lt;= to_date(#{endTime},'yyyy-mm-dd')
            </if>
        </where>
        <!-- 数据范围过滤 -->
        ${params.dataScope}
        order by r.role_sort
    </select>

    <select id="selectRolePermissionByUserId" parameterType="java.lang.String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE ur.user_id = #{userId}
    </select>

    <select id="selectRoleAll" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
    </select>

    <select id="selectRoleListByUserId" parameterType="java.lang.String" resultType="java.lang.String">
        select r.id
        from sys_role r
                 left join sys_user_role ur on ur.role_id = r.id
                 left join sys_user u on u.id = ur.user_id
        where u.id = #{userId}
    </select>

    <select id="selectRoleById" parameterType="java.lang.String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.id = #{id}
    </select>

    <select id="selectRolesByUserName" parameterType="java.lang.String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        WHERE u.user_name = #{userName}
    </select>

    <select id="checkRoleNameUnique" parameterType="java.lang.String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_name=#{roleName}
    </select>

    <select id="checkRoleKeyUnique" parameterType="java.lang.String" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_key=#{roleKey}
    </select>

    <insert id="insertRole" parameterType="cn.silver.framework.system.domain.SysRole"
    >
        insert into sys_role(
        id,
        <if test="roleName != null and roleName != ''">role_name,</if>
        <if test="roleKey != null and roleKey != ''">role_key,</if>
        <if test="roleSort != null and roleSort != ''">role_sort,</if>
        <if test="dataScope != null and dataScope != ''">data_scope,</if>
        <if test="menuCheckStrictly != null">menu_check_strictly,</if>
        <if test="deptCheckStrictly != null">dept_check_strictly,</if>
        <if test="status != null and status != ''">status,</if>
        <if test="remark != null and remark != ''">remark,</if>
        <if test="createBy != null and createBy != ''">create_by,</if>
        <if test="createName != null and createName != ''">create_name,</if>
        <if test="updateBy != null and updateBy != ''">update_by,</if>
        <if test="updateName != null and updateName != ''">update_name,</if>
        create_time,update_time
        )values(
        #{id},
        <if test="roleName != null and roleName != ''">#{roleName},</if>
        <if test="roleKey != null and roleKey != ''">#{roleKey},</if>
        <if test="roleSort != null and roleSort != ''">#{roleSort},</if>
        <if test="dataScope != null and dataScope != ''">#{dataScope},</if>
        <if test="menuCheckStrictly != null">#{menuCheckStrictly},</if>
        <if test="deptCheckStrictly != null">#{deptCheckStrictly},</if>
        <if test="status != null and status != ''">#{status},</if>
        <if test="remark != null and remark != ''">#{remark},</if>
        <if test="createBy != null and createBy != ''">#{createBy},</if>
        <if test="createName != null and createName != ''">#{createName},</if>
        <if test="updateBy != null and updateBy != ''">#{updateBy},</if>
        <if test="updateName != null and updateName != ''">#{updateName},</if>
        sysdate,sysdate
        )
    </insert>

    <update id="updateRole" parameterType="cn.silver.framework.system.domain.SysRole">
        update sys_role
        <set>
            <if test="roleName != null and roleName != ''">role_name = #{roleName},</if>
            <if test="roleKey != null and roleKey != ''">role_key = #{roleKey},</if>
            <if test="roleSort != null and roleSort != ''">role_sort = #{roleSort},</if>
            <if test="dataScope != null and dataScope != ''">data_scope = #{dataScope},</if>
            <if test="menuCheckStrictly != null">menu_check_strictly = #{menuCheckStrictly},</if>
            <if test="deptCheckStrictly != null">dept_check_strictly = #{deptCheckStrictly},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="updateName != null and updateName != ''">update_name = #{updateName},</if>
            update_time = sysdate
        </set>
        where id = #{id}
    </update>

    <delete id="deleteRoleById" parameterType="java.lang.String">
        delete from sys_role
        where id = #{id}
    </delete>

    <delete id="deleteRoleByIds" parameterType="java.lang.String">
        delete from sys_role where id in
        <foreach collection="collection" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
