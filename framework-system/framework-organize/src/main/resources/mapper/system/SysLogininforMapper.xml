<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.silver.framework.system.mapper.SysLogininforMapper">

    <resultMap type="cn.silver.framework.system.domain.SysLogininfor" id="SysLogininforResult">
        <id property="id" column="id"/>
        <result property="userName" column="user_name"/>
        <result property="status" column="status"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>
    </resultMap>

    <insert id="insertLogininfor" parameterType="cn.silver.framework.system.domain.SysLogininfor"
    >
        insert into sys_logininfor (id, user_name, status, ipaddr, login_location, browser, os, msg, login_time)
        values (#{id}, #{userName}, #{status}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{msg}, sysdate)
    </insert>

    <select id="selectLogininforList" parameterType="cn.silver.framework.system.domain.SysLogininfor"
            resultMap="SysLogininforResult">
        select id, user_name, ipaddr, login_location, browser, os, status, msg, login_time from sys_logininfor
        <where>
            <if test="ipaddr != null and ipaddr != ''">
                AND ipaddr like concat(concat('%', #{ipaddr}), '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
            <if test="userName != null and userName != ''">
                AND user_name like concat(concat('%', #{userName}), '%')
            </if>
            <if test="beginTime != null and beginTime != ''"><!-- 开始时间检索 -->
                and to_date(to_char(login_time,'yyyy-mm-dd'),'yyyy-mm-dd') &gt;= to_date(#{beginTime},'yyyy-mm-dd')
            </if>
            <if test="endTime != null and endTime != ''"><!-- 结束时间检索 -->
                and to_date(to_char(login_time,'yyyy-mm-dd'),'yyyy-mm-dd') &lt;= to_date(#{endTime},'yyyy-mm-dd')
            </if>
        </where>
        order by id desc
    </select>

    <delete id="deleteLogininforByIds" parameterType="java.lang.String">
        delete from sys_logininfor where id in
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <update id="cleanLogininfor">
        truncate table sys_logininfor
    </update>

</mapper>
